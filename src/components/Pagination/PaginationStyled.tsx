import styled from "styled-components";

const PaginationStyled = styled.button`
  width: fit-content;
  margin-bottom: 1rem;
  border: none;
  font-weight: 600;
  background-color: #fafafa;
  color: #0569ff;
  cursor: pointer;
`;

export default PaginationStyled;
